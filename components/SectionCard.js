'use client'; import { motion } from 'framer-motion'; export default function SectionCard({title,children,className=''}){ return (<motion.section initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} transition={{type:'spring',stiffness:60,damping:12}} viewport={{once:true}} className={`card ${className}`}><h2 className="text-2xl font-semibold mb-4">{title}</h2><div>{children}</div></motion.section>); }