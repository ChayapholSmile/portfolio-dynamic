import mongoose from 'mongoose'; const FileSchema = new mongoose.Schema({ name:String, mime:String, base64:String },{_id:false}); const ProfileSchema = new mongoose.Schema({ displayName:String, bio:String, avatar:FileSchema, social:Object, createdBy:{type:mongoose.Schema.Types.ObjectId,ref:'User'}, updatedAt:{type:Date,default:Date.now} }); export default mongoose.models.Profile || mongoose.model('Profile', ProfileSchema);