import mongoose from 'mongoose'; const FileSchema = new mongoose.Schema({ name:String, mime:String, base64:String },{_id:false}); const ProjectSchema = new mongoose.Schema({ title:String, organization:String, location:String, startDate:Date, endDate:Date, description:String, link:String, attachments:[FileSchema], createdBy:{type:mongoose.Schema.Types.ObjectId,ref:'User'}, createdAt:{type:Date,default:Date.now} }); export default mongoose.models.Project || mongoose.model('Project', ProjectSchema);