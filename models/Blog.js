import mongoose from 'mongoose'; const FileSchema = new mongoose.Schema({ name:String, mime:String, base64:String },{_id:false}); const BlogSchema = new mongoose.Schema({ title:String, slug:{type:String,unique:true}, excerpt:String, content:String, cover:FileSchema, tags:[String], author:{type:mongoose.Schema.Types.ObjectId,ref:'User'}, createdAt:{type:Date,default:Date.now} }); export default mongoose.models.Blog || mongoose.model('Blog', BlogSchema);